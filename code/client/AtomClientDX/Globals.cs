namespace AtomClient {
	public class Globals {
		static public bool CAMERA_FOLLOWS_MAYA = false;
		static public cameras.Camera DEFAULT_CAMERA = new cameras.Camera();

		static readonly public object LOCK_OBJ = new object();
	}
}
